import { wrapChannelSplitterNode } from '../wrappers/channel-splitter-node';
export var createNativeChannelSplitterNodeFactory = function (createNativeAudioNode) {
    return function (nativeContext, options) {
        var nativeChannelSplitterNode = createNativeAudioNode(nativeContext, function (ntvCntxt) {
            return ntvCntxt.createChannelSplitter(options.numberOfOutputs);
        });
        // Bug #29, #30, #31 & #32: Only Chrome, Firefox & Opera partially support the spec yet.
        wrapChannelSplitterNode(nativeChannelSplitterNode);
        return nativeChannelSplitterNode;
    };
};
//# sourceMappingURL=/build/es2015/factories/native-channel-splitter-node-factory.js.map